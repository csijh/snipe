
// Scan a line of text, using a language-specific table, and a start state,
// using the type array for output, and the stack for bracket matching. The
// type array and stack should be pre-allocated to be big enough.
int scan(byte *table, int state, char *line, byte *types, Bracket *stack) {

}
